{
  "name": "strata.foundation.core",
  "private": true,
  "version": "0.0.6",
  "description": "",
  "scripts": {
    "clean": "del-cli -f build/dist",
    "copy-package": "shx cp main/typescript/package.json build/dist/package.json",
    "build": "tsc && yarn run copy-package && cd build/dist && yarn pack",
    "publish-package": "cd build/dist && yarn publish",
    "new-patch": "yarn version --patch",
    "new-minor": "yarn version --minor",
    "new-major": "yarn version --major",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "rxjs": "^7.8.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
